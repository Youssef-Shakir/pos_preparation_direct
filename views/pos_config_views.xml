<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Extend POS Config Form -->
    <record id="pos_config_view_form_inherit" model="ir.ui.view">
        <field name="name">pos.config.form.inherit.preparation.direct</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='other_devices']" position="after">
                <setting string="Preparation Printing" help="Print preparation tickets to kitchen/bar printers">
                    <field name="use_preparation_printing"/>
                    <div class="content-group mt16" invisible="not use_preparation_printing">
                        <div class="row">
                            <label for="preparation_printer_ids" class="col-lg-4 o_light_label"/>
                            <field name="preparation_printer_ids" widget="many2many_tags" class="col-lg-8"/>
                        </div>
                    </div>
                </setting>
            </xpath>
        </field>
    </record>

</odoo>
